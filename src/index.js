const { path } = require('./pathon');
const { mutablePreset, immutablePreset } = require('./presets');

module.exports.path = path;
module.exports.mutablePreset = mutablePreset;
module.exports.immutablePreset = immutablePreset;
